# a lambda function to calculate the square value.

#lambda function -This creates an anonymous function (a function without a name) using the lambda keyword.

f = lambda x: x * x
value = f(5)
print('Square of value is', value)

"""
def square(x):
   res=x*x

"""